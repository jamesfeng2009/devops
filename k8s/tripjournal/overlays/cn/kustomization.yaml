# cn overlay：调度封装 + 国内入口（Ingress-Nginx + LE 证书）
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tripjournal
resources:
  - ../../base
  - certificate.yaml
  - sealed-secret.yaml
patches:
  - path: patches.yaml
